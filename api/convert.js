// replace existing weapon/loot handling with:
if(loot==="sword"){ p *= 0.05 * (1/3); if(we==="yes") p *= 1.25; }
else if(loot==="shovel"){ p *= 0.05 * (2/3); if(we==="yes") p *= 1.25; }
else if(loot==="can pick up"){ p *= 0.55; }
